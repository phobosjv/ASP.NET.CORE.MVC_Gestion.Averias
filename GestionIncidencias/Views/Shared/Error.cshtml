@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<h2 class="text-danger">Error.</h2>
<h2 class="text-danger">Ha ocurrido un error mientras se procesaba la petición.</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>Request ID:</strong> <code>@Model.RequestId</code>
    </p>
}

<h3>Development Mode</h3>
<p>
    Si es el desarrollador, y desea obtener más información, cambie la aplicación a modo 'Development'.
</p>
<p>
    <strong>El entorno de desarrollo no debe estar habilitado para aplicaciones implementadas.</strong>
    Puede dar lugar a que se muestre información confidencial de excepciones a los usuarios finales.
    Para la depuración local habilite el entorno <strong>Development</strong>, estableciendo la variable de entorno <strong>ASPNETCORE_ENVIRONMENT</strong> a <strong>Development</strong>
    Finalmente reinicie la aplicación.
</p>
